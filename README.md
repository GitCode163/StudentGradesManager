# StudentGradesManager
Student Grades is a simple Java program that uses arrays to store student names and their grades.
The user enters how many students there are, then types each name and grade.
The program saves this data in two arrays and prints a clear list of results.
